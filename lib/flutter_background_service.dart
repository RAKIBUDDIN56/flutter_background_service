import 'package:flutter/material.dart';

class FlutterBackgroundService extends StatelessWidget {
  const FlutterBackgroundService({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Flutter local notification')),
    );
  }
}
